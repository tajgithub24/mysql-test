apiVersion: v1
kind: Service
metadata:
  name: {{ include "mysql-test-chart.fullname" . }}
  labels:
    {{- include "mysql-test-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mysql-test-chart.selectorLabels" . | nindent 4 }}
